# アルゴリズムチートシート(JS)
|やりたいこと|ソース|
|---|---|
|半角スペース区切りlineを変数格納|`const [S, X] = lines[0].split(' ');`|
|`number`で受け取る|`const [N, X] = lines[0].split(' ').map(Number);`|
|配列内の最大値を取得|`const maxSize = Math.max(...aList);`|
|配列内の最大値を取得2(降順ソートしてしまう)|`const aList = lines[1].split(' ').map(Number).sort((a, b) => a - b);`|
